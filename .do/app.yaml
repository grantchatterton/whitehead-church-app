name: whitehead-church-app
services:
  - name: web
    envs:
      - key: MONGODB_URI
        value: ${MONGODB_URI}
        type: SECRET
        scope: RUN_TIME
    github:
      branch: main
      repo: grantchatterton/whitehead-church-app
