name: whitehead-church-app
services:
  - name: web
    envs:
      - key: MONGODB_URI
        value: ${MONGODB_URI}
        type: SECRET
        scope: RUN_AND_BUILD_TIME
    http_port: 3000
    github:
      branch: main
      repo: grantchatterton/whitehead-church-app
